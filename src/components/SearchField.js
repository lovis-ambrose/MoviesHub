import React from "react";

const SearchMovie = (props) => {
    return (
        <div className="col col-sm-4">
            <input className="form-control" placeholder="search for a movie..."></input>
        </div>
    );
};

export default SearchMovie;